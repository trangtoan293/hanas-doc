apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: dbt-backfill-run
  namespace: spark-jobs
  labels:
    app: dbt-spark
    deployment: backfill
spec:
  type: Python
  mode: cluster
  image: "trangtoan293/dbt-spark-k8s-ktl:ktl-dbt"
  imagePullPolicy: IfNotPresent
  mainApplicationFile: "local:///opt/spark/work-dir/dbt-project/dbt-project/dbt_runner.py"
  arguments: 
    - "--use-subprocess"
    - "--dbt-command"
    - "ktl_dbt"
    - "--upload-artifacts"
    - "--s3-bucket"
    - "data"
    - "--s3-endpoint-url"
    - "http://192.168.1.151"
    - "--s3-prefix"
    - "de_team/dbt_artifacts"
    - "--"
    - "run"
    - "--target"
    - "dev"
    - "--select"
    - "{{ params.dbt_select }}"
    - "--full-refresh"
  sparkVersion: "3.5.1"

  sparkConf:
    # Optimizations
    "spark.sql.adaptive.enabled": "true"
    "spark.sql.adaptive.coalescePartitions.enabled": "true"
    "spark.serializer": "org.apache.spark.serializer.KryoSerializer"

    # Hive Metastore & Warehouse
    "spark.hadoop.hive.metastore.uris": "thrift://192.168.1.156:9083"
    "spark.sql.warehouse.dir": "s3a://data/warehouse/"

    # S3/MinIO (no credentials embedded)
    "spark.hadoop.fs.s3a.endpoint": "http://192.168.1.151"
    "spark.hadoop.fs.s3a.path.style.access": "true"
    "spark.hadoop.fs.s3a.ssl.enabled": "false"
    "spark.hadoop.fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
    "spark.hadoop.fs.s3a.aws.credentials.provider": "org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider"
    "spark.hadoop.fs.s3a.access.key": "Vl21ZDUEq1XNiDDN"
    "spark.hadoop.fs.s3a.secret.key": "Su9omOppqPvl9q59ABdIfzwOzDi1PpgZ"

    # Iceberg
    "spark.sql.extensions": "org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions"
    "spark.sql.catalog.spark_catalog": "org.apache.iceberg.spark.SparkSessionCatalog"
    "spark.sql.catalog.spark_catalog.type": "hive"

    # Kubernetes
    "spark.kubernetes.namespace": "spark-jobs"
 
    # Demo Catalog (Custom Iceberg)
    "spark.sql.catalog.demo": "org.apache.iceberg.spark.SparkCatalog"
    "spark.sql.catalog.demo.type": "hive"
    "spark.sql.catalog.demo.uri": "thrift://192.168.1.156:9083"
    "spark.sql.catalog.demo.io-impl": "org.apache.iceberg.aws.s3.S3FileIO"
    "spark.sql.catalog.demo.s3.endpoint": "http://192.168.1.151"
    "spark.sql.catalog.demo.warehouse": "s3a://data/warehouse/"
    "spark.sql.catalog.demo.metrics-reporter-impl": "org.apache.iceberg.metrics.LoggingMetricsReporter"
    # Default Catalog
    "spark.sql.defaultCatalog": "demo"

  driver:
    cores: {{ dag_run.conf.get('backfill_dbt_driver_cores', params.backfill_dbt_driver_cores) | int }}
    memory: "{{ dag_run.conf.get('backfill_dbt_driver_memory', params.backfill_dbt_driver_memory) }}"
    serviceAccount: spark

    # Sidecar: git-sync keeps repo up-to-date (not just one-time)
    initContainers:
      - name: git-sync
        image: registry.k8s.io/git-sync/git-sync:v4.1.0
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 1001
          runAsGroup: 1001
        envFrom:
          - configMapRef:
              name: git-sync-config
              optional: false
        volumeMounts:
          - name: dbt-project
            mountPath: /opt/spark/work-dir
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"

    env:
      - name: SCHEMA_NAME
        value: "{{ params.dbt_schema }}"
        
    envFrom:
      - secretRef:
          name: spark-k8s-aws-credentials
      - secretRef:
          name: spark-k8s-oracle-credentials
      - secretRef:
          name: spark-k8s-mssql-credentials
      - configMapRef:
          name: spark-k8s-config

    volumeMounts:
      - name: dbt-project
        mountPath: /opt/spark/work-dir

    labels:
      component: spark-driver

  executor:
    cores: {{ dag_run.conf.get('backfill_dbt_executor_cores', params.backfill_dbt_executor_cores) | int }}
    memory: "{{ dag_run.conf.get('backfill_dbt_executor_memory', params.backfill_dbt_executor_memory) }}"
    instances: {{ dag_run.conf.get('backfill_dbt_executor_instances', params.backfill_dbt_executor_instances) | int }}
    envFrom:
      - secretRef:
          name: spark-k8s-aws-credentials
      - secretRef:
          name: spark-k8s-oracle-credentials
      - secretRef:
          name: spark-k8s-mssql-credentials
      - configMapRef:
          name: spark-k8s-config
    
    env:
      - name: SCHEMA_NAME
        value: "{{ params.dbt_schema }}"

    volumeMounts:
      - name: dbt-project
        mountPath: /opt/spark/work-dir
    labels:
      component: spark-executor

  volumes:
    - name: dbt-project
      emptyDir: {}

  restartPolicy:
    type: OnFailure
    onFailureRetries: 0
    onFailureRetryInterval: 10
    onSubmissionFailureRetries: 0
    onSubmissionFailureRetryInterval: 20
